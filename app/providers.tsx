"use client";

// import { ReactLenis } from "react-lenis";

export function Providers({ children }: { children: React.ReactNode }) {
    return (
        // <ReactLenis root>
            {children}
        // </ReactLenis>
    );
}
